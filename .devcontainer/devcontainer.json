{
  "name": "OSH CLI DevContainer",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "postCreateCommand": "pip install --upgrade pip && pip install -e .",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  },
  "remoteUser": "vscode"
}
